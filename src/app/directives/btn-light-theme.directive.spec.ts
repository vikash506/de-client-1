import { BtnLightThemeDirective } from './btn-light-theme.directive';

describe('BtnLightThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnLightThemeDirective();
    expect(directive).toBeTruthy();
  });
});
