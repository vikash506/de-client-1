import { PgThemeDirective } from './pg-theme.directive';

describe('PgThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new PgThemeDirective();
    expect(directive).toBeTruthy();
  });
});
