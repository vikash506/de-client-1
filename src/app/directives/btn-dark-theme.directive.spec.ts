import { BtnDarkThemeDirective } from './btn-dark-theme.directive';

describe('BtnDarkThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnDarkThemeDirective();
    expect(directive).toBeTruthy();
  });
});
